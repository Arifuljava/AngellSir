<resources>

    <!-- Base application theme. -->
    <style name="MainStyle" parent="Theme.AppCompat.NoActionBar"/>

    <style name="anim_bottom_pop">

        <item name="android:windowEnterAnimation">@anim/botoom_pop_in</item>

        <item name="android:windowExitAnimation">@anim/botoom_pop_out</item>

    </style>

    <style name="My_CheckBox" parent="Widget.AppCompat.CompoundButton.RadioButton">
        <item name="colorControlActivated">@color/bacgroundColor</item>
        <item name="colorControlNormal">@color/bacgroundColor</item>
    </style>


</resources>
